<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>パスワード再設定リクエスト</title>
    <link rel="stylesheet" th:href="@{/css/style.css}" />
</head>
<body>
    <div class="nagoyameshi-reset-wrapper">
        <div class="nagoyameshi-reset-card">
            <h2 class="text-center mb-4">パスワードをお忘れですか？</h2>

            <p class="text-center">登録済みのメールアドレスを入力してください。<br>再設定用のリンクをメールでお送りします。</p>

            <!-- メッセージ表示 -->
            <div th:if="${successMessage}" class="alert alert-success" th:text="${successMessage}"></div>
            <div th:if="${errorMessage}" class="alert alert-danger" th:text="${errorMessage}"></div>

            <form th:action="@{/password/reset/request}" method="post">
                <div class="form-group mb-3">
                    <input type="email" id="email" name="email" class="form-control" placeholder="メールアドレス" required />
                </div>
                <div class="form-group text-center">
                    <button type="submit" class="btn nagoyameshi-btn w-100">再設定メールを送信</button>
                </div>
            </form>

            <div class="text-center mt-3">
                <a th:href="@{/login}">ログイン画面に戻る</a>
            </div>
        </div>
    </div>
</body>
</html>
